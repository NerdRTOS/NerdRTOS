

/*

// memheap init
// nd_err_t nd_memheap_init(nd_memheap_t *heap, const char *name, void *start_addr, nd_size_t size)

// memheap alloc
// void* nd_memheap_alloc(nd_memheap_t *heap, nd_size_t size);

// memheap free
// void nd_memheap_free(nd_memheap_t *heap, void *ptr);

// memheap realloc
// void* nd_memheap_realloc(nd_memheap_t *heap, void *ptr, nd_size_t new_size);

// memheap destroy
// void nd_memheap_destroy(nd_memheap_t *heap);

// memheap get free size
// nd_size_t nd_memheap_free_size(nd_memheap_t *heap);

// memheap get total size
// nd_size_t nd_memheap_total_size(nd_memheap_t *heap);

*/
