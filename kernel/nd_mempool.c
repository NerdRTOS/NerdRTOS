
/*

// Memory pool initialization
// nd_err_t nd_mempool_init(nd_mempool_t *pool, const char *name, void *start_addr, nd_size_t block_size, nd_size_t block_count);

// Allocate a block from the memory pool
// void* nd_mempool_alloc(nd_mempool_t *pool);

// Free a block in the memory pool
// void nd_mempool_free(nd_mempool_t *pool, void *ptr);

// Destroy the memory pool
// void nd_mempool_destroy(nd_mempool_t *pool);

// Get the number of remaining allocatable blocks in the memory pool
// nd_size_t nd_mempool_free_count(nd_mempool_t *pool);

// Get the total number of blocks in the memory pool
// nd_size_t nd_mempool_total_count(nd_mempool_t *pool);

*/
